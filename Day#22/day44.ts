//Question 130

import { add } from './44export';
console.log(add(23,5));//you may try but anyhow only calling the function is not working , though we need to call it via console.log.

//Question 131

// In another file:
import { Person } from "./day44export";

const alice = new Person("Alice");
alice.greet(); // Outputs: Hello, my name is Alice
// This snippet imports the Person class and uses it to create an instance.
